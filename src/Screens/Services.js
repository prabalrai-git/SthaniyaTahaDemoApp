import {Text} from 'react-native';

export function Services() {
  return (
    <Text
      style={{
        fontSize: 20,
        color: 'black',
        textAlign: 'center',
        marginTop: 100,
      }}>
      सेवाहरू
    </Text>
  );
}
