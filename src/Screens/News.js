import {Text} from 'react-native';

export function News() {
  return (
    <Text
      style={{
        fontSize: 20,
        color: 'black',
        textAlign: 'center',
        marginTop: 100,
      }}>
      समाचार
    </Text>
  );
}
